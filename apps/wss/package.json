{
  "name": "wss",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun server.ts --watch --hot --no-clear-screen",
    "prod": "bun server.ts",
    "test": "bun test",
    "test:watch": "bun test:watch",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@repo/websocket": "*",
    "@repo/draft-engine": "*",
    "@types/bun": "^1.3.1"
  }
}
